#![forbid(unsafe_code)]
//! Non-claiming Phase 0C executable boundary for `ptah-control`.

fn main() {
    eprintln!("ptah-control: Phase 0C scaffold only; runtime implementation is not authorized.");
}
