name: TEMP C06 head carrier marker
# disposable synchronization marker; base-side workflow performs the build

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  marker:
    runs-on: ubuntu-24.04
    steps:
      - run: echo 'C06 head carrier only; product publication removes this file.'
