{
  "schema_version": "0.1.0",
  "record_type": "ptah.e01.secure_multi_node_acceptance_corpus",
  "accepted_predecessor": "f22d23c9bbf3c9c43884535e3483486c4bc0826f",
  "roadmap_authority": "98dc8c4e8639cda80510bee0625db34b4fdf9384",
  "protocol_id": "ptah.node.link.v1",
  "first_transport": "tls13_mutual_authentication_over_tcp",
  "scheduler_added": false,
  "transfer_plane_added": false,
  "overlay_transport_added": false,
  "automatic_discovery_added": false,
  "local_first_reconciliation_added": false,
  "new_core_entity_required": false,
  "frozen_contract_change_required": false,
  "cases": [
    {"id":"two_node_concurrency","class":"positive","expected_result":"two canonical Nodes hold independent current authenticated sessions","required_evidence":["services/ptah-control/tests/e01_node_link.rs::two_nodes_are_accepted_as_independent_current_sessions"]},
    {"id":"reconnect","class":"recovery","expected_result":"higher ConnectionEpoch replaces the prior session without changing NodeId","required_evidence":["crates/ptah-node-link/tests/enrollment_session.rs::reconnect_supersedes_old_epoch_and_generation"]},
    {"id":"node_restart","class":"recovery","expected_result":"A02 restart preserves NodeId and advances Generation and ConnectionEpoch","required_evidence":["ptah-node-agent A02 restart regression suite"]},
    {"id":"control_restart","class":"recovery","expected_result":"a restarted control registry does not treat an old binding as current authority","required_evidence":["services/ptah-control/tests/e01_node_link.rs::control_restart_fences_previous_in_memory_binding"]},
    {"id":"credential_rotation","class":"recovery","expected_result":"overlap accepts the replacement credential and revoked credential authority is fenced","required_evidence":["crates/ptah-node-link/tests/enrollment_session.rs::credential_rotation_preserves_node_identity_and_fences_revoked_key"]},
    {"id":"plaintext_rejected","class":"negative","expected_result":"plaintext cannot downgrade the TLS listener","required_evidence":["crates/ptah-node-link/tests/tls_transport.rs::plaintext_cannot_downgrade_tls_listener"]},
    {"id":"wrong_ca_rejected","class":"negative","expected_result":"untrusted server or client certificate chains fail closed","required_evidence":["crates/ptah-node-link/tests/tls_transport.rs::wrong_server_trust_root_fails_closed","crates/ptah-node-link/tests/tls_transport.rs::client_signed_by_wrong_ca_fails_server_authentication"]},
    {"id":"wrong_enrollment_binding_rejected","class":"negative","expected_result":"a Hello carrying a different enrollment reference is rejected","required_evidence":["crates/ptah-node-link/tests/enrollment_session.rs::wrong_enrollment_reference_fails_closed"]},
    {"id":"revoked_enrollment_rejected","class":"negative","expected_result":"revoked enrollment lifecycle cannot establish a session","required_evidence":["crates/ptah-node-link/tests/enrollment_session.rs::lifecycle_and_identity_fail_closed"]},
    {"id":"expired_enrollment_rejected","class":"negative","expected_result":"expired enrollment lifecycle or deadline cannot establish a session","required_evidence":["crates/ptah-node-link/tests/enrollment_session.rs::lifecycle_and_identity_fail_closed","crates/ptah-node-link/tests/enrollment_session.rs::expired_deadline_fails_closed"]},
    {"id":"stale_generation_rejected","class":"negative","expected_result":"older Node Generation is rejected against current authority","required_evidence":["crates/ptah-node-link/tests/enrollment_session.rs::reconnect_supersedes_old_epoch_and_generation"]},
    {"id":"stale_or_equal_epoch_rejected","class":"negative","expected_result":"equal or older ConnectionEpoch cannot replay current authority","required_evidence":["crates/ptah-node-link/tests/enrollment_session.rs::reconnect_supersedes_old_epoch_and_generation"]},
    {"id":"superseded_publish_rejected","class":"negative","expected_result":"superseded session cannot publish capability truth","required_evidence":["services/ptah-control/tests/e01_node_link.rs::superseded_session_cannot_publish_capability"]},
    {"id":"capability_identity_mismatch_rejected","class":"negative","expected_result":"capability NodeId Generation or ConnectionEpoch must match the authenticated binding","required_evidence":["services/ptah-control/tests/e01_node_link.rs::capability_must_match_authenticated_node_authority"]},
    {"id":"malformed_frame_rejected","class":"negative","expected_result":"malformed frame input fails closed","required_evidence":["crates/ptah-node-link/tests/framing.rs::zero_length_frame_is_malformed"]},
    {"id":"oversized_frame_rejected","class":"negative","expected_result":"declared frame larger than the fixed E01 bound is rejected before payload read","required_evidence":["crates/ptah-node-link/tests/framing.rs::oversized_declared_frame_fails_before_payload_read"]},
    {"id":"unsupported_protocol_rejected","class":"negative","expected_result":"incompatible protocol major fails closed","required_evidence":["crates/ptah-node-link/tests/protocol.rs::incompatible_major_fails_closed"]}
  ]
}
